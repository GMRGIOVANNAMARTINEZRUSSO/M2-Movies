console.log(tempData);

// contenedor tarjetas de películas
const container = document.getElementById('container');
//$.get("url", (data)=>{
    //renderCards(data)
//})

//const renderCards= ()=>{

// Mapea tarjeta para cada película
